# S 点复用新浪 upbs `'0'` 值，不自研卖出算法

观察池的「S 点」（卖出信号）直接采用新浪财经 upbs 多空信号接口的 `value='0'`，与 B 信号（`'1'`）同源同接口，后端不做任何卖出侧计算。依据：抽样 7 只票验证 B/S 严格交替出现且与新浪K线图 B/S 标记口径一致（用户肉眼确认）；后端扫描已全量落库所有信号值，S 检测零新增抓取成本。代价：信号口径完全绑定新浪，接口变更需跟随。

**Considered Options**：自研卖出规则（如均线/顶背离）被否决——与 B 信号口径不对称，且引入计算与验证成本，违反 YAGNI。
